// The Conslusion //



Limitations of computing:	

			Hardware:
				one main reason there are limitations in computing is numbers are infinite, but the repersentation of them within the computer is not.
				second reason, hardware is just that, hardware!
		
				Limits on Arithimetic:
							Integer Numbers:
									Ref Henry Walker The Limits of Computing, integer numbers can get big very fast example: using a chess board,
									place a single kernel of corn on each square and the double it for the next square, grand total you'd have 
//									8x10¹⁸, or 110,000 bushels of corn. Another example, but a real world example this time, if a computer word
									was 64 bits and we repersent only positive numbers, we could just repersent the number of kernels on the 64th
									square. Overflow would occur. Alhough there are software solutions that can overcome these limititations, example,
									repersenting a very large number as a list of smaller numbers, example, 
//																		a) number = 752,036													//
//																			number 7 -> 5 -> 2 -> 0 -> 3 -> 6										//
																																	
//																		b) number = 752,036													//
//																			number 752 -> 036												//
	
//																		c) sum = 83536 + 41													//
//																			num 1: 8 -> 3 -> 5 -> 3 -> 6 											//
//																		 	num 2: 4 -> 1													//
//																			num 3: 8 -> 3 -> 5 -> 7 -> 7											//

							Real Numbers:
									Real numbers are stored as a integer along with information showing where the radix point is. Looking at a Radix-Point
									lets say each memory location is the same size and is divided into a sign plus five decimal digits, when a variable or 
									constant is assigned the location assigned to it consists of five digits and a sign. When an integral variable or a constant 
									is assigned, the number stored in that palce is straightforward. When a real variable is declared or a real constant is defined,
									the number stored there has both a whole number part and a fraction part. The number must be coded to repersent both parts.
									Example, 
										-99999 Largest negative number
										+00000 Zero
										+99999 Largest positive number
										The precision(the maximum number of digits that can be repersented) is five digits, and each number within that range can
										be repersented exactly. What Happens when we allow oen of these digits(let's say the leftmost one) to repersent an exponent,
											example,
												+32345	repersents the number +2345*10³ (keep in mind that the leftmost digit "3" is repersenting an exponent)
												The range of numbers we can now repersent is much larger:
																			-9999*10⁹ to +9999*10⁹ 
												Now, the precision is only four digits. We can only repersent four significant digits(nonzero digits or zero digits
													that are exact) of the number itself. This means we can repersent only four-digit numbers exactly in our system.
													In Larger numbers the four leftmost digits are correct, and the balance of the digits are assumed to be zero. We
													lose the rightmost, or least significant, digits.
												example,
													Number 	Sign  Exp. 		Value
														 ↓	↓			
													+99999	 +	1   9999	+99999
												       -999999	 -	2   9999	-999999
												      +1000000   +	3   1000	+1000000
												      -4932416   -	3   4932	-4932000
																		Notice we can repersent exactly 1000000, but not -4932416. Our coding scheme
																			is limited to four significant digits; the digits we cannot repersent
																			are assumed to be zero. To extend our coding scheme to repersent real
																			numbers, we need to be able to repersent negative exponents,
																		example,
																			4394*10ˉ³= 43.94
																			or
																			22*10ˉ⁴=0.0022
																					Because our scheme does not allow for a sign for the exponent,
																					we have to change the scheme slightly. Let's let the sign that
																					we have already been using be the sign of the exponent and add a
																					sign to the left of it to be the sign of the number itself.
																					example,
																						+-99999 Largest positive number +9999*10⁹
																						↑↑
																					sign of #, sign of exponent
																						--99999 Largest negative number -9999*10ˉ⁹
																						++99999 Smallest positive number +1*10⁹
																						--90001 Smallest negative number -1*10ˉ⁹
																					Now we can repersent all of the numbers between -9999*10ˉ⁹ and 9999*10⁹
																					accurately to four digits, including all the fractional values.
																						Suppose we want to add three numbers x, y and z using this coding scheme.
																						By adding y and z, then adding x to result, The associative law of arithmetic 
																						says that the two answers should be the same-but are they?
																						example,
																							x=-1324*10³ y=1325*10³ z=5424*10⁰
																							
																							lets look at the result of adding z to the sum of x and y:
																								x+y=1*10³=1000*10⁰
																								x+y=1000*10⁰
																								  z=5424*10⁰
																									result is 6424*10⁰=(x+y)+z
																							lets look see what happens when we add x to the sum of y and z:
																								y+z=1330424*10⁰=1330*10³(truncated to four digits)
																								y+z=1330*10³
																								  x=-1324*10³
																									result is 6*10³=6000*10⁰=x+(y+z)
																									Our answers are the same in the thousands' place but are
																									different in the hundreds', tens', and ones' place, this is 
																									called repersentational error or round-off error. The result
																									of adding y to z gives us a number with seven digits of precision,
																									but only four can be stored. In addition to repersentational errors,
																									there are two other problems to watch out for in floating-point arithmetic:
																										underflow and overflow.
																									Underflow is the condition that arises when the absolute value of a calculation
																									gets to small to be repersented. for example if we had a 10­¹³ our minimum is -9,
																									therefore the result would be set to zero.
																									Overflow is the condition that arises when the absolute value of a calculation gets
																									too large to be repersented. Overflow is a more serious problem, because 10⁺²¹, our 
																									minimum is -9, so the result of the calculation cannot be stored, which we can at this
																									point issue a 10⁹ or issue a cancellation error, which can happen with floating-point
																									numbers of widely differring magnitudes when they are added or subtracted.

				Limits on communications:
								
							Parity Numbers:
									Parity bits are used to detect that an error has occured between the storing and retrieving of a byte, or the sending and recieving of a byte. 
									A parity bit is an extra bit that is associated with each byte in the hardware that uses the scheme, used to ensure that the number of 1 bits in 
										a 9-bit value(byte plus parity bit) is odd(or even) across all bytes.
										Odd parity:	requires the number of 1s in a byte plus parity bit to be odd.
										Even parity:	 requires the numbers of 1s in a byte plus parity to be even.
											Both even and odd parity use this scheme,
																if a byte contains the pattern // 11001100 //, the parity bit would be 1, giving an odd 
																number of 1s. If the pattern were // 11110001 // the parity bit would be 0 giving an odd
																number of 1s. When a byte is retrieved from memory or recieved from a transmission, the 
																number f 1 bits is counted(including the parity bit). If the number is even, an error has
																occurred. If the scheme is used in the hardware each byte actually has an extra bit,
																accessible only by the hardware, that is used for error detection.

						********Check digits:
									A software variation of the same scheme is to sum the individual digits of a number, and then store the unit's digit of that sum with the number.
										Ex:	
											// given the number 34376, the sum of the digits is 23, so the number would be stored as 34376-3. If the 4 became corrupted as a 3, //
											// the error would be detected. the scheme could be expanded to carry an additional digit, say the units digit of the sum of the odd //
											// digits, in this case we will say 34376 would be stored as 34376-23: 3 is the units digits of the sum of all the digits and 2 is the //
											// units digit of sum of the 1st, 3rd, and 5th digit.
										NOTE:
											The more important it is for errors to be deteected the more complex the alorithm used to detect them.
											* LOOK INTO THIS ALORITHM LATER ON.
							
							Error-Correcting Codes:
										If enough information about a byte or number is kept, it possible to deduce what an incorrect bit or digit must be. The ultimate redundancy is to
										keep two copies of every value that is stored, if the parity is in error or there is an error in the check digits, look back at the extra copy to 
										determine the correct value. 
				Current Approaches to Software Quality:

							Software Engineering:
										Two verification techniques effectively used by programming teams design/code, walk-throughs/inspections both responsible for uncovering errors.
										walk-through the team performs a manual simualtion of the deisgn or program with sample test inputs, keeping track of the programmers data by hand
										on paper or blackboard.
										inspection a reader(never the programmers author) goes through the requirements, design, or code line by line, pointing out the errors, recorded on
										an inspection report, and read aloud to uncover any other errors that maybe underlining, and prove errors long before the testing stage(walk-through) 
										begins. 
										NOTE:
											We have spoke about large software projects, lets quantify what we mean by large, The Space Shuttle Ground Processing System has over a half million
											lines of code; Windows 95 has ten million lines of code; Most large projects fall somewhere in between.
												Standard software:	25 bugs per 1000 lines of source code
												Good software:	2 errors per 1000 lines of source code
												Space Shuttle software:	< 1 error per 10000 lines of source code (KSLOC errors per source lines of code)
												The goal of this research is to establish a method for proving programs that is analogous to the method for correctness mathamatically, theorectically,
												and  statistically.

				Problems:
						
							Comparing Algorithms:
										1. Execution Time (Latency)
										//		long startTime = System.nanoTime();	//
										//		// run algorithm			//
										//		long endTime = System.nanoTime();	//
										//		long duration = endTime - startTime;	//

										2. Memory Usage
										//		Runtime runtime = Runtime.getRuntime(runtime.gc());			//
										//		long usedMemoryBefore = runtime.totalMemory() - runtime.freeMemory();	//
										//		// run algorithm							//
										//		runtime.gc();								//
										//		long usedMemoryAfter = runtime.totalMemory() - runtime.freeMemory();	//
										//		long memoryUsed = usedMemoryAfter - usedMemoryBefore; // in bytes	//

										3. Accuracy
										//		// Example: Accuracy			
										//		int correct = 0;						//
										//		for (int i = 0; i < trueLabels.length; i++) {			//
										//		if (predictedLabels[i] == trueLabels[i]) {			//
										//		correct++;							//
										//			}							//
										//		}								//
										//		double accuracy = (double) correct / trueLabels.length;		//

										4. throughput
										//		long startTime = System.nanoTime();				//
										//		// run algorithm						//
										//		long endTime = System.nanoTime();				//
										//		double seconds = (endTime - startTime) / 1e9;			//
										//		double throughput = numberOfOperations / seconds;		//

										5. Convergence or Stability Metrics
										//		// Pseudocode	<----don't really need this line from gpg
										//		while (difference > threshold && iterations < maxIterations) {	//
										//		// perform iteration						//
										//		difference = Math.abs(newValue - oldValue);			//
										//		iterations++;							//
										//		}								//
							
										6. defining a int for Objective measures			
										//		public interface AlgorithmMeasure {				//
										//		double measure(AlgorithmResult result);				//
										//		}								//
						
										The reason for this is to compare two algorithms that do the same task, and understand
										the complexity of algorithms, forms a continuum from easy to unsolvable
										Execution times are specific to a particular computer.
										For a list of 100 elements, there are 99 addition operations, however we do not count
										the number of addition operations; it is some function of the elements  (N) in the list, 
										therefore, we can express the number of addition operations in terms of N: for a list
										of N elements, there are N-1 addition operations.
							Big-O Analysis:	
									Common Big-O Notations:
												O(1) - Constant Time Complexity
												O(log n) - Lagarithmic Time Complexity
												O(n) - Linear Time Complexity
												O(n log n) - Linearithmic Time Complexity
												O(n^2) - Quadratic Time Complexity
												O(2^n) - Exponential Time Complexity
												O(n!) - Factorial Time Complexity
												
										//		public class Example {						//		
										//			public static void main(String[] args) {		//
										//				int[] array = new int[1000];			//
										//				for (int i = 0; i < array.length; i++) {	//
										//					for (int j = 0; j < array.length; j++) {	//
										//						array[j] = array[j] + 1;	//
										//						}				//
										//					}					//
										//				}						//
										//			}							//	
										
										Example time complexity in java called BigOTest
										//		public class BigOTest {								//
										//			public static void main(String[] args) {				//
										//				int[] inputSizes = {100, 1000, 10_000, 100_000, 1_000_000};	//
										//												//
										//				for (int size : inputSizes) {					//
										//					long startTime = System.nanoTime();			//
										//												//
										//				// Call the method you want to test				//
										//				testMethod(size);						//
										//												//
										//				long endTime = System.nanoTime();				//
										//				long duration = endTime - startTime;				//
										//												//
										//				System.out.printIn("Input size: " + size + " - Time:		//
										//					}							//
										//				}								//
										//												//
										//				// Example method: nested loops(0(n^2))				//
										//				public static void testMethod(int size) {			//
										//					int[] array = new int[size];				//
										//												//
										//					for (int i = 0; i < array.length; i++) {		//
										//						for (int j = 0; j < array.length; j++) {	//
										//							array[j] = array[j] + 1;		//
										//								}				//
										//							}					//
										//						}						//
										//					}							//
										Output:
										//				Input size:	100 - Time: 12345 ns
										//				Input size:	1000 - Time: 678901 ns
										//				Input size:	10000 - Time 7890123 ns
										//				Input size:	100000 - Time: 89012345 ns
										// 				Input size:	1000000 - Time: 901234567 ns
										